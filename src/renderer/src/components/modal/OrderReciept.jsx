import { Box } from '@chakra-ui/react'
import React from 'react'
import { Flex } from '@chakra-ui/react'
const OrderReciept = ({ orderInfo, orderData, orderPayment }) => {
  return (
    <Box maxWidth={'400px'} overflow={'auto'}>
      <Flex></Flex>
    </Box>
  )
}

export default OrderReciept
